zerops:
  - setup: app
    build:
      base: nodejs@20
      buildCommands:
        - yarn install --immutable
        - yarn nuxi generate
      deployFiles:
        - .output/public/~
    run:
      base: static
